services:
  - type: web
    name: my-stripe-api # You can name this whatever you like
    env: python
    plan: free # Use Render's free tier
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --workers 4 --bind 0.0.0.0:$PORT app:app"
    healthCheckPath: /
